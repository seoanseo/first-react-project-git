import React, { CSSProperties } from 'react';
import { LazyModule } from '../constants.js';
export default function RenderIslandSeparately({ id, lazyModule, componentProps, WrapperElement, wrapperStyle, wrapperClassName, Wrapper, }: {
    id: string;
    lazyModule: LazyModule<any>;
    componentProps: any;
    WrapperElement: keyof JSX.IntrinsicElements;
    wrapperStyle?: CSSProperties;
    wrapperClassName?: string;
    Wrapper?: LazyModule<React.ComponentType<{
        children: JSX.Element;
    }>>;
}): JSX.Element;
