import { CSSProperties } from 'react';
import { HydrationType, LazyIslandModule } from './constants.js';
declare type IslandProps<TProps> = {
    id?: string;
    wrapperTag?: keyof JSX.IntrinsicElements;
    wrapperStyle?: CSSProperties;
    wrapperClassName?: string;
    Wrapper?: LazyIslandModule<{
        children: JSX.Element;
    }>;
    hydrateOn?: HydrationType;
    clientOnly?: boolean;
    priority?: number;
    module: LazyIslandModule<TProps>;
} & TProps;
declare function Island<TProps>({ id, wrapperTag: WrapperElement, wrapperStyle, wrapperClassName, module, hydrateOn, clientOnly, priority, Wrapper, ...moduleProps }: IslandProps<TProps>): JSX.Element;
export default Island;
