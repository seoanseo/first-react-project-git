import { Request, Response, NextFunction } from 'express';
declare const appServerRouteHandler: (req: Request, res: Response, __next: NextFunction) => Promise<void>;
export default appServerRouteHandler;
