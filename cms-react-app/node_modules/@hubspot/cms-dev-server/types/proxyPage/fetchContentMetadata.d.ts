import { Hublet } from '@hubspot/cms-components';
export declare type PortalContentIdAndHublet = {
    portalId: number;
    contentId: string;
    hublet: Hublet;
};
export default function fetchContentMetadata(url: string, portalId: number): Promise<PortalContentIdAndHublet>;
