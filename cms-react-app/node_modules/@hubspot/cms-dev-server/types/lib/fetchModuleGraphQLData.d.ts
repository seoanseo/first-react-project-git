declare type DataQueryPoints = {
    count: number;
    weight: number;
    used_points: number;
};
declare type DataQueryResult = {
    extensions: {
        query_complexity: {
            max_points: number;
            used_points: number;
            points_for_internal_api_requests: DataQueryPoints;
            points_for_objects_retrieved: DataQueryPoints;
            points_for_properties_with_value: DataQueryPoints;
            points_for_properties_without_value: DataQueryPoints;
        };
    };
    data?: {
        CRM?: any;
        HUBDB?: any;
    };
};
export declare function clearGraphQLDataCache(): void;
export default function fetchModuleGraphQLData(accountId: number, query: string, requestedModulePath: string, pageUrl: string): Promise<DataQueryResult>;
export {};
