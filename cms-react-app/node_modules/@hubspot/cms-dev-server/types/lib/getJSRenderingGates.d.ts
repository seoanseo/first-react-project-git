import { Gate } from '@hubspot/cms-js-internal';
import { HSEnvironment } from '@hubspot/cms-components';
declare type EnabledFeatures = {
    [name: string]: number;
};
export declare const mapEnabledFeaturesToGates: (enabledFeatures: EnabledFeatures) => Gate[];
export default function getJsRenderingGates(portalId: number, env: HSEnvironment, personalAccessKey: string): Promise<Gate[]>;
export {};
